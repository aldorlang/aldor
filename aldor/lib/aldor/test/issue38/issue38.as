#include "aldor"
#include "aldorio"

import from MachineInteger;

main(): () == {
        stdout <<  min << newline;
        stdout <<  0 << newline;
        stdout <<  1 << newline;
        stdout <<  max << newline;
}
main();
